
  # Physical Networking App Layout

  This is a code bundle for Physical Networking App Layout. The original project is available at https://www.figma.com/design/kaTmc3WCUPaV8GM1ZyxR2k/Physical-Networking-App-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
